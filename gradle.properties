org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Advanced Solar Panels is a 1.12.2 IC2 addon (decompiled + deobfuscated from 4.3.0).
mod_version=4.3.0

minecraft_version=1.12.2
forge_version=14.23.5.2847

# Mapping set the sources were deobfuscated with (mcp_stable-39-1.12). Must match the
# CSVs used for deobfuscation, otherwise some MCP method names won't line up (e.g.
# NBTTagCompound.isEmpty() in stable_39 vs hasNoTags() in snapshot_20171003).
mapping_version=stable_39

# IC2 Experimental dev jar (provides ic2.core.* internals used by the mod).
ic2_version=2.8.222-ex112

# JEI for 1.12.2 (JEICompat compiles against mezz.jei.api.*).
jei_version=4.16.1.1013
